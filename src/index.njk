---
title: "Photo Box"
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title }}</title>
    <link rel="stylesheet" type="text/css" href="chaffinchAsset(/tailwind.css)"/>
    <link rel="stylesheet" type="text/css" href="chaffinchAsset(/main.css)"/>
  </head>
  <body>
    <main class="w-full p-8">
      <h1 class="w-full mx-auto p-4 text-4xl lg:text-3xl font-thin font-sans">{{ title }}</h1>
      {% for image in images %}
        {% set path %}./img/{{image.file}}{% endset %}
        {% set abspath %}./src/img/{{image.file}}{% endset %}
        <figure class="mx-auto p-2 block lg:flex items-end justify-center" >
          <div class="flex-1 pb-4 lg:border-b border-black">
            <div
              data-src="img-1200({{ path }})" 
              data-xs="img-400({{ path }})"
              data-sm="img-600({{ path }})"
              data-md="img-800({{ path }})"
              data-lg="img-1200({{ path }})"
              data-alt="{{image.description}}" 
              data-aspect={{ abspath | aspect }} 
              data-bg="{{abspath | blurPreview }}"
              class="lazy w-full h-full"
            >
              <noscript>
                <img src="chaffinchAsset({{ path }})" alt="{{image.description}}"></img>
              </noscript>
            </div>

          </div>
          <figcaption class="lg:pl-4 w-full lg:w-40 text-3xl lg:text-sm align-baseline flex flex-0 lg:block items-start justify-between w-40 ">
            <div class="order-2 text-right lg:text-left">
              <p class="uppercase hidden lg:block tracking-wide text-gray-900 lg:text-xs">{{ abspath | exif(["image.Make", "image.Model"]) }}</p>
              <p class="text-gray-800 lg:text-xs whitespace-no-wrap">
                <span class="text-gray-700 tracking-wide">FL </span>
                {{ abspath | exif("exif.FocalLength") }}
              &bull;
              <span class="text-gray-700 tracking-wide ">A </span>
                {{ abspath | exif("exif.FNumber") }}
              &bull;
              <span class="text-gray-700 tracking-wide ">Sh. </span>
                {{ abspath | exif("exif.ShutterSpeedValue") }}</p>
            </div>
            <div class="order-1">
              <h2 class="max-w-sm">{{image.description}}</h2>
              <p class="italic text-gray-800 lg:text-sm">{{ abspath | exif("exif.DateTimeOriginal") | date("",":") }}</p>
            </div>
          </figcaption>
        </figure>
      {% endfor %}

    </main>
    <script defer src="chaffinchAsset(/index.js)"></script>
  </body>
</html>